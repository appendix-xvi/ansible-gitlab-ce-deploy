# Security Notes

- Do not commit secrets, SSH private keys, or local credential files.
- Limit SSH access to trusted source IPs and administrators.
- Use HTTPS and valid certificates for real usage beyond a local lab.
- Secure backup files with strict permissions and controlled storage.
- Rotate the initial GitLab root password immediately after first login.
- Apply firewall rules to expose only required ports.
